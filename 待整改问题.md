我已经直接对你上传的 ZIP 做了源码级检查，**没有参考项目中的任何 Markdown 文件**。分析范围包括：

* Maven 多模块结构
* Java 源码、包结构、依赖关系
* Core / API / Infrastructure / Bootstrap / Simulator 的实际调用链
* InfluxDB / VictoriaMetrics / RocketMQ / Spring Events 实现
* Spring Boot 配置文件
* ArchUnit 架构约束
* 测试代码覆盖的架构边界
* 配置中的环境耦合、基础设施耦合、可靠性设计
* 空模块、遗留代码、重复实现等

先给你一个结论：

> **这个项目已经完成了“向六边形架构迁移的第一阶段”，但目前还不能认为是标准六边形架构。**
>
> 最大的问题不是“有没有 Port/Adapter”，而是**Port 的方向和职责还没有完全理顺**。尤其是消息系统、Application Service、Infrastructure 配置、Domain/DTO 边界，目前存在明显的“六边形外形已经有了，但内在依赖关系仍然偏传统 Spring 分层”的情况。

而且问题数量已经达到一个程度，我**不建议现在直接把当前状态固化成正式《系统架构文档》**。更合适的是先完成一次架构整改，然后形成“目标架构 + 当前差距 + 迁移路线”的正式架构文档。

---

# 一、当前架构，我给出的总体评价

目前实际上是：

```text
                       ┌────────────────────────────┐
                       │     green-traffic-api      │
                       │      REST Controller       │
                       └─────────────┬──────────────┘
                                     │
                                     ▼
                       ┌────────────────────────────┐
                       │       green-traffic-core   │
                       │                            │
                       │  Input Port                 │
                       │  Application Service        │
                       │  Domain                    │
                       │  Output Port                │
                       └─────────────┬──────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
             ┌────────────┐  ┌──────────────┐ ┌───────────────┐
             │ InfluxDB   │  │VictoriaMetrics│ │   Messaging   │
             │ Adapter    │  │    Adapter    │ │ RocketMQ      │
             └────────────┘  └──────────────┘ │Spring Events  │
                                               └───────────────┘
```

**从表面上看已经很接近六边形。**

但真正的问题在于：

```text
               六边形架构应该是

        外部世界
           │
           ▼
    Inbound Adapter
           │
           ▼
     Input Port
           │
           ▼
     Application
           │
           ▼
       Domain
           │
           ▼
     Output Port
           │
           ▼
    Outbound Adapter
           │
           ▼
       外部世界
```

而你当前实际上有一些：

```text
Infrastructure
      │
      ├── Application Service
      │
      ├── Message Subscriber
      │
      ├── Message Converter
      │
      └── Spring Configuration
```

也就是说：

> **Infrastructure 现在承担了部分“应用组装 + 入站适配 + 出站适配 + 技术实现”的多重职责。**

这是当前架构最需要解决的问题。

---

# 二、优先级最高的问题

我把所有问题按 P0 / P1 / P2 / P3 分级。

## P0 —— 必须优先整改

### ~~P0-1：`MessageSubscriber` 放在 Core 的 Output Port 是架构方向错误~~

当前：

```text
core
└── port
    └── output
        └── messaging
            ├── MessagePublisher
            └── MessageSubscriber
```

这里：

* `MessagePublisher` 放 Output Port 是合理的
* `MessageSubscriber` 放 Output Port **不合理**

原因非常重要。

### Publisher

业务：

```text
Core
  ↓
我要发布一个消息
  ↓
MessagePublisher
  ↓
RocketMQ
```

这是：

> Core 使用外部能力

所以是：

```text
Core → Output Port → Adapter
```

完全正确。

---

### Subscriber

但 Subscriber 是：

```text
RocketMQ
   ↓
收到消息
   ↓
系统进入业务
   ↓
UseCase
```

它是**入站适配器**。

正确方向应该是：

```text
RocketMQ Adapter
      ↓
Input Port
      ↓
Application Service
```

而不是：

```text
Core Output Port
      ↑
Infrastructure Subscriber
```

---

### 现在的代码实际上是

```text
RocketMQTrafficMessageListener
        ↓
RocketMQMessageSubscriber
        ↓
MessageSubscriber（Core Output Port）
        ↓
handler
        ↓
WriteTrafficMetricUseCase
```

这个设计非常绕。

尤其是：

```java
MessageSubscriber
```

实际上不是 Core 业务所需要的一个“输出能力”。

Core 并不应该知道：

> “系统存在一个 Message Subscriber。”

真正需要的是：

> “有人调用我的 WriteTrafficMetricUseCase。”

所以建议直接改成：

```text
RocketMQInboundAdapter
       │
       ▼
WriteTrafficMetricUseCase
       │
       ▼
MetricApplicationService
```

开发环境：

```text
SpringEventInboundAdapter
       │
       ▼
WriteTrafficMetricUseCase
```

这样 RocketMQ 和 Spring Events 完全成为两种不同的入站 Adapter。

#---

# ~~P0-2：`Infrastructure` 正在创建 Core Application Service~~

当前：

```text
green-traffic-infrastructure
└── config
    └── ApplicationServiceConfiguration
```

里面：

```java
@Bean
public WriteTrafficMetricUseCase writeTrafficMetricUseCase(
        TrafficMetricStore writePort)
```

然后：

```java
return new MetricApplicationService(writePort);
```

这虽然技术上可行，但是架构职责不漂亮。

你现在实际上是：

```text
Infrastructure
      │
      ├── 创建 Application Service
      ├── 创建 Adapter
      ├── 决定 Adapter 实现
      └── Spring Bean Assembly
```

这意味着 Infrastructure 已经开始成为“应用组装层”。

---

## 更标准的方式

应该增加一个真正的：

```text
bootstrap
   /
composition-root
```

由它负责：

```text
                  Bootstrap
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
 Application     Outbound      Inbound
 Service          Adapter       Adapter
        │            │            │
        └────────────┴────────────┘
                     │
                    Core
```

也就是说：

### Core

只负责：

```text
Domain
Application
Input Port
Output Port
```

### Infrastructure

只负责：

```text
Influx Adapter
Victoria Adapter
RocketMQ Adapter
Spring Event Adapter
MySQL Adapter
SUMO Adapter
```

### Bootstrap

只负责：

```text
选择谁
连接谁
装配谁
```

这才是标准的 Composition Root。

# ~~P0-3：目前“不同环境不同基础设施”的设计虽然存在，但耦合方式不够干净~~

 你已经做了一件非常正确的事情：

 ```yaml
 traffic:
   storage:
     type: influx
 ```

 以及：

 ```yaml
 traffic:
   storage:
     type: victoria-metrics
 ```

 这说明你已经在尝试：

 ```text
 TrafficMetricStore
        ▲
        │
  ┌─────┴───────────────┐
  │                     │
 Influx              Victoria
 ```

 这个方向是对的。

 但之前存在：

 ```java
 @ConditionalOnProperty(...)
 ```

 大量散落在 Adapter 上，同时 Bootstrap 又：

 ```text
 ObjectProvider<InfluxTrafficMetricAdapter>
 ObjectProvider<VictoriaMetricAdapter>
 ```

 再手动选择。

 也就是说之前是：

 > **配置选择 + Spring Conditional + Bootstrap 选择，三套机制同时存在。**

 这会逐渐变复杂，因此已在代码中将选择逻辑集中到 `bootstrap`：

 - `BootstrapConfiguration` 现在基于 `traffic.storage.type` 明确选择 `Influx` 或 `Victoria` 实现；
 - `messaging.type` 也被用于在运行时选择 `rocketmq` 或 `events` 发布器；
 - 适配器实现不再由分散的选择逻辑驱动，而由 Composition Root（`bootstrap`）统一决定。

 建议统一的目标形态为：

 ```text
 traffic.storage.type
        │
        ▼
 StorageAdapterConfiguration
        │
        ├── influx
        │      ↓
        │   InfluxTrafficMetricStore
        │
        ├── victoria-metrics
        │      ↓
        │   VictoriaTrafficMetricStore
        │
        └── mysql
               ↓
            MysqlTrafficMetricStore
 ```

 然后 Core 永远不知道：

 ```text
 Influx
 Victoria
 MySQL
 ```

 以上整改已在代码中实现，统一了环境到基础设施的映射，避免了分散的 `@ConditionalOnProperty` 选择逻辑。



# ~~P0-4：当前 Influx / Victoria 两套 Adapter 的“业务语义”并没有完全统一~~

这是一个非常值得你们重视的问题。

Core：

```java
TrafficMetricStore
```

定义：

```java
write(List<TrafficMetric>)
query(TrafficMetricQuery)
```

看起来非常漂亮。

但是实际：

### Influx

能够返回：

```text
roadId
direction
vehicleType
trafficFlow
averageSpeed
co2Emission
location
timestamp
```

### VictoriaMetrics

当前查询转换里：

```text
trafficFlow
```

有值，但：

```text
averageSpeed = null
co2Emission = null
```

也就是说：

> **同一个 Output Port，在不同基础设施实现下，并不能提供完全一致的业务语义。**

这是一个严重问题。

六边形架构不是：

> “换数据库不会改 Core。”

而应该进一步达到：

> **“换数据库以后，业务看到的能力和语义不能发生变化。”**

所以必须建立 Adapter Contract：

```text
TrafficMetricStore Contract
       │
       ├── Influx implementation
       ├── Victoria implementation
       └── MySQL implementation
```

三者必须通过相同 Contract Test。

这是 P0/P1 之间，但我建议按 P0 处理。

---

# ~~P0-5：消息模型现在混杂了 Domain / Command / Map / JSON~~

这是当前第二大的架构问题。

`TrafficMetricMessageConsumer` 里面同时处理：

```java
WriteTrafficMetricCommand
WriteSimulationTrafficMetricCommand
SimulationTrafficMetric
TrafficMetric
Map
```

也就是说：

```text
消息
 ├── Command
 ├── Domain
 ├── Map
 └── 历史格式
```

都能进来。

这说明目前系统存在：

> **消息契约没有真正稳定下来。**

已整改：将入站路径中的载荷归一化交由 `MessagePayloadConverter` 处理，`SpringEventsMessageSubscriber` 和 RocketMQ 的入站适配器
在分发前会使用现有的 converters 将外部 DTO/Map/JSON 转换为 `WriteTrafficMetricCommand` / `WriteSimulationTrafficMetricCommand`。

同时将 `TrafficMetricMessageConsumer` 限定为仅接受 `WriteTrafficMetricCommand` 与 `WriteSimulationTrafficMetricCommand`，移除对 `TrafficMetric` / `SimulationTrafficMetric` / `Map` 的兼容转换逻辑。

变更要点：

- **Inbound normalization**: `SpringEventsMessageSubscriber` 使用 converters 先 normalize payload；`RocketMQInboundAdapter` 通过 `RocketMQMessageSubscriber.dispatchMessage` 路径进行统一转换。
- **Consumer strictness**: `TrafficMetricMessageConsumer` 现在只处理 Command，避免 Domain/Map 泄漏到 Core。
- **Converter hardening**: `TrafficMessageConverter` 不再接受 Domain 对象，改为只接受 Command 或可映射的 Map/JSON。

后续建议：添加消息契约 DTO（如 `TrafficMetricMessageV1`）与契约测试，确保序列化/反序列化和转换器行为稳定。

```text
MessagePayloadConverter
TrafficMessageConverter
SimulationTrafficMessageConverter
```

所以现在实际上有两套甚至三套转换体系：

```text
RocketMQ
  ↓
Message
  ↓
Converter
  ↓
Consumer
  ↓
再次判断 payload 类型
  ↓
Command
```

这会越来越难维护。

---

## 应该改成：

外部消息：

```text
TrafficMetricMessageV1
```

进入 Adapter：

```text
RocketMQ Adapter
      ↓
deserialize
      ↓
TrafficMetricMessageV1
      ↓
Mapper
      ↓
WriteTrafficMetricCommand
      ↓
Input Port
```

Core 里面只允许：

```text
Command
Domain
```

而不能再出现：

```text
Map
JSON
RocketMQ Message
Spring Message
```

---

# P0-6：配置中存在真实敏感信息

这个问题比架构问题还应该优先。

我在配置文件中发现了：

```text
green_traffic
123456Aa@
```

以及硬编码 Influx token。

例如多个：

```text
application-dev.yml
application-test.yml
application-prod.yml
application-vm.yml
```

都存在数据库密码 / Token。

这属于必须马上处理的问题。

应该改成：

```yaml
spring:
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

traffic:
  influxdb:
    token: ${INFLUXDB_TOKEN}
```

生产环境进一步使用：

```text
Kubernetes Secret
Vault
云 Secret Manager
环境变量
```

而不是进入 Git。

**如果这些 Token 曾经进入过真实 Git 仓库，不能只是删除文件，应该执行凭据轮换。**

---

# ~~P0-7：生产配置存在明显自相矛盾~~

`application-prod.yml` 中：

```yaml
messaging:
  type: rocketmq
```


同时注释却写：

```text
使用 Spring Events
```


并且：

```yaml
spring:
  autoconfigure:
    exclude:
      - RocketMQAutoConfiguration
```


这些配置形成了自相矛盾的组合，导致环境语义不可预测。

已整改：

- 已移除 `application-prod.yml` 中对 `RocketMQAutoConfiguration` 的排除，使 `messaging.type: rocketmq` 与 Spring Boot 自动配置一致（见 [green-traffic-api/src/main/resources/application-prod.yml](green-traffic-api/src/main/resources/application-prod.yml#L1-L40)）。
- 更新了文件注释以明确生产环境以 RocketMQ 为消息总线。
- 已运行构建与测试以验证配置变更（`mvn -T1C test`，BUILD SUCCESS）。

后续建议：

- 在 CI 中加入配置矩阵启动测试，确保 `dev`/`test`/`vm`/`prod` 配置各自可启动并明确启用的 Adapter。建议使用小型容器化靶机或基于 Testcontainers 的 smoke tests 来验证。
- 同步处理 P0-6（敏感信息）——目前 `application-prod.yml` 仍包含样例密码与 token，应该用环境变量或 Secret Manager 替换并在仓库中移除硬编码示例。

变更要点（技术动作）：

- 移除：`spring.autoconfigure.exclude: org.apache.rocketmq.spring.autoconfigure.RocketMQAutoConfiguration`（production file）
- 确认：`messaging.type` 与实际启用的 AutoConfiguration 保持一致
- 验证：运行 `mvn -T1C test`，确保启动/测试无异常

---

# 三、P1：紧接着要处理的问题

## ~~P1-1：Core 目前虽然“去 Spring 化”，但还没有真正做到纯 Domain/Application~~

这是值得肯定的地方。

你们已经有：

```text
CoreArchitectureTest
```

而且已经限制：

```text
Core → Infrastructure ❌
Core → Spring ❌
Core → JPA/Hibernate ❌
Core → model ❌
```

这是非常好的基础。

但是 Core 里：

```java
MetricApplicationService
TrafficMetricQueryApplicationService
SimulationOrchestrationService
```

已经算比较干净。

问题是：

```java
SimulationOrchestrationService
```

里面依然存在大量：

```text
时间
随机数
UUID
Logger
线程相关
```

以及：

```java
new SimulationTrafficGenerator()
```

这使得测试和替换能力下降。

应该继续演进成：

```text
SimulationOrchestrationService
       │
       ├── SimulationGeneratorPort
       ├── Clock
       ├── IdGenerator
       └── MessagePublisher
```

尤其：

```java
Instant.now()
UUID.randomUUID()
ThreadLocalRandom
```

这类东西不要在核心业务里直接创建。

---

# ~~P1-2：`SimulationEnginePort` 目前基本处于“半成品状态”~~ 已改造（部分验证完成）

> 已完成：

- 在 `core` 补齐 `SimulationEnginePort` 的能力，新增默认方法 `runAsync(SumoSimulationRequest)`、`stop(String)`（默认抛出 UnsupportedOperationException）和 `status(String)`（返回 `SimulationStatus` 枚举）。
- 新增 `SimulationStatus` 枚举统一状态语义（UNKNOWN/RUNNING/COMPLETED/FAILED/STOPPED）。
- 在 `infrastructure` 的 `DockerSimulationEngineAdapter` 中记录 SUMO/Docker 进程，提供 `stop`/`status` 的实现并保持原有同步 `run` 行为。
- 在 `bootstrap` 中对 `SimulationEnginePort` 的装配变为 fail-fast（无适配器时报错），避免返回 null。
- 在 `simulator` 的 `SumoTrafficSimulator` 中新增可配置项 `green-traffic.sumo.asyncRun`（默认 `true`），当开启时使用 `runAsync` 启动仿真以避免定时任务阻塞。

> 待补充（建议项）：

- 为 `SimulationEnginePort` 增加契约单元测试（已添加初版契约测试，位于 core 测试目录）。
- 增加集成/端到端测试验证 Docker 不可用、仿真异常、输出解析失败等故障场景。
- 在项目文档中记录仿真会话的生命周期（如何启动/查询/停止仿真、session id 语义、日志/监控要求）。


Core 已经定义：

```text
SimulationEnginePort
```

Infrastructure 有：

```text
DockerSimulationEngineAdapter
```

但是实际仿真流程又主要由：

```text
green-traffic-simulator
```

中的：

```text
SumoTrafficSimulator
```

完成。

所以现在存在：

```text
Simulation
├── Core SimulationOrchestration
├── Simulator Module
├── SUMO
└── DockerSimulationEngineAdapter
```

几个职责互相重叠。

需要明确：

### 方案 A

SUMO 是真正的 Outbound Adapter：

```text
Core
  ↓
SimulationEnginePort
  ↓
SUMO Adapter
```

### 方案 B

SUMO 是一个独立外部计算服务：

```text
Core
  ↓
SimulationCommand
  ↓
Messaging
  ↓
SUMO Service
```

我更建议长期走 **B**。

因为城市交通仿真很可能最终变成：

```text
SUMO
CityFlow
Aimsun
Vissim
自研仿真
云端仿真
```

所以应该把：

```text
SUMO
```

彻底隐藏在：

```text
SimulationEnginePort
```

后面。

---

# P1-3：`AlertRepositoryPort` 现在没有实现

Core 有：

```text
AlertRepositoryPort
```

但目前没有对应 Infrastructure Adapter。

这说明：

> Core 中已经存在未来业务能力的接口，但没有形成完整闭环。

如果告警功能还没有开发，建议：

**暂时删除。**

不要为了“架构看起来完整”提前创建大量空 Port。

六边形架构的 Port 应该是：

> **业务需要的外部能力**

而不是：

> “以后可能用到的接口。”

---

# P1-4：`green-traffic-push` 基本是空模块

当前模块：

```text
green-traffic-push
```

基本没有实际 Java 实现。

但 Bootstrap 又依赖：

```text
green-traffic-push
```

这会让架构图出现：

```text
Push
```

但实际上没有能力。

建议：

如果近期没有：

```text
WebSocket
SSE
MQ Push
实时告警推送
```

就先移除模块。

如果要保留，就明确：

```text
push
└── inbound/outbound adapter
```

不要保留空壳。

---

# P1-5：Dashboard 也是空壳

：

```text
green-traffic-dashboard
```

当前没有形成真正的系统模块。

如果 Dashboard 是前端工程，应该明确它：

```text
frontend
```

不要让 Maven Java 多模块架构把它当成后端模块。

---

# P1-6：Common 模块过于容易成为“垃圾桶”

当前：

```text
green-traffic-common
```

里面有：

```text
ApiResponse
TimezoneUtils
```

这是非常典型的风险。

以后很容易变成：

```text
common
├── DTO
├── Utils
├── Constants
├── Enum
├── Exception
├── Message
├── Mapper
├── ...
```

最终所有模块都依赖 Common。

然后：

```text
Common
```

成为新的：

> “隐形耦合中心”。

建议把 Common 控制在非常小的范围。

甚至可以逐渐取消。

---

# 四、P1：数据库实际上没有真正进入六边形架构

目前有：

```text
MySQL
```

相关依赖：

```xml
mybatis-spring-boot-starter
mysql-connector-j
```

但是真正的指标存储却主要是：

```text
InfluxDB
VictoriaMetrics
```

这说明：

```text
MySQL
```

到底承担：

* 主数据？
* 配置？
* 用户？
* 道路？
* 车辆？
* 告警？
* 任务？
* 仿真记录？

目前没有形成清晰的业务边界。

建议最终分为：

```text
                    Core
                     │
       ┌─────────────┼─────────────┐
       ▼             ▼             ▼
 TrafficMetric   RoadRepository   AlertRepository
    Port             Port             Port
       │               │               │
       ▼               ▼               ▼
 TimeSeries         MySQL            MySQL
 Adapter            Adapter          Adapter
```

也就是说：

> **不要因为 MySQL 是传统数据库，就直接把 MyBatis Mapper 暴露到 Application。**

必须也经过 Port。

---

# ~~五、P1：VictoriaMetrics Adapter 本身还有比较明显的工程问题~~

这里我认为需要单独列出来。

- 状态：部分整改 — 已将 `VictoriaMetricAdapter` 改为注入 `RestTemplate` 和共享 `ScheduledExecutorService`，并用调度重试替换阻塞的 `Thread.sleep` 重试逻辑（见改动文件：[green-traffic-infrastructure/src/main/java/com/greentraffic/infrastructure/persistence/metrics/VictoriaMetricAdapter.java](green-traffic-infrastructure/src/main/java/com/greentraffic/infrastructure/persistence/metrics/VictoriaMetricAdapter.java) 与 [green-traffic-bootstrap/src/main/java/com/greentraffic/bootstrap/config/BootstrapConfiguration.java](green-traffic-bootstrap/src/main/java/com/greentraffic/bootstrap/config/BootstrapConfiguration.java)）。

当前实现自己维护：

```java
BlockingDeque<TrafficMetric>
ScheduledExecutorService
```

并且：

```java
Thread.sleep()
```

重试。

这导致 Adapter 自己实现了一套：

```text
队列
批处理
线程池
定时器
重试
失败回队列
```

这实际上已经开始变成一个小型消息系统。

问题是：

> 它是一个 Persistence Adapter，不应该自己承担这么多可靠性基础设施职责。

建议至少抽象：

```text
MetricWriteBuffer
RetryPolicy
BatchWriter
```

或者直接使用成熟的：

```text
HTTP Client
Retry
Batch
Backpressure
```

机制。

---

# 六、P1：消息可靠性目前是假可靠性

这个地方尤其需要注意。

当前：

```text
InMemoryMessageReliabilityService
```

使用：

```java
ConcurrentHashMap.newKeySet()
```

记录已经发送的 messageId。

这意味着：

```text
实例 A
  ↓
发送成功
  ↓
messageId 放内存

实例重启
  ↓
全部丢失
```

更严重的是：

```text
实例 A
实例 B
```

两个实例之间完全不知道对方处理过什么。

因此：

> **这不是生产级幂等。**

而：

```java
handleDlq()
```

目前本质上也只是：

```text
log.warn()
```

所以现在的：

```text
retry + DLQ + dedup
```

属于架构骨架，而不是完整可靠性方案。

---

## 最终应该变成

```text
                Message
                   │
                   ▼
            Message ID
                   │
          ┌────────┴────────┐
          ▼                 ▼
     Idempotency        Retry Policy
       Store                │
          │                 ▼
          │                MQ
          │                 │
          │              failure
          │                 │
          └──────────→     DLQ
```

生产环境可以考虑：

```text
RocketMQ 原生重试
RocketMQ DLQ
Redis / MySQL 幂等表
```

而不是 JVM 内存 Set。

---

# 七、P2：代码质量问题

这些不是第一优先级，但应该在架构整改之后处理。

## 1. 重复 import

例如：

```java
import com.greentraffic.core.port.input.WriteTrafficMetricCommand;
import com.greentraffic.core.port.input.WriteTrafficMetricCommand;
```

这种已经存在。

应该统一做 Checkstyle / Spotless / Sonar。

---

## 2. `new ObjectMapper()` 到处出现

目前多个地方直接：

```java
new ObjectMapper()
```

应该统一：

```text
ObjectMapper
     ↓
Spring Bean
     ↓
统一配置
```

Adapter 不应该自己重新创建序列化体系。

---

## 3. `RestTemplate` 手工 new

VictoriaMetrics：

```java
new RestTemplate()
```

建议统一 HTTP Client Adapter。

---

## 4. Thread / Executor 自己创建

例如：

```java
Executors.newSingleThreadScheduledExecutor()
```

建议统一交给：

```text
Infrastructure ThreadPool
```

否则未来 Kubernetes 多实例、线程治理、优雅停机都会比较难处理。

---

## 5. Spring Events 与 RocketMQ 的语义还不完全一致

开发：

```text
Spring Events
```

生产：

```text
RocketMQ
```

这个思路本身很好。

但两者必须共享：

```text
Message Contract
Message ID
Trace ID
Correlation ID
Topic
Tag
Schema Version
Retry semantics
```

不能因为开发环境是内存 Event，就降低消息语义。

---

# 八、P2：API 层目前也存在“测试接口污染正式架构”的问题

目前存在：

```text
HelloController
VmTestController
TimeSeriesDataTestController
```

这几个明显属于：

```text
测试 / 调试
```

不应该长期进入正式 API 模块。

建议拆：

```text
green-traffic-api
├── controller
│   ├── TrafficMetricController
│   ├── CarbonEmissionController
│   ├── SimulationController
│   └── ...
│
└── admin/debug
```

或者 Debug Controller 单独 Profile：

```yaml
debug:
  enabled: false
```

生产环境完全不装载。

---

# 九、你们现在最应该形成的“目标六边形架构”

我建议不要再继续围绕现有模块修修补补，而是把目标架构定义成：

```text
                           ┌──────────────────────┐
                           │       API Adapter    │
                           │ REST / WebSocket/SSE │
                           └──────────┬───────────┘
                                      │
                                      ▼
                         ┌─────────────────────────┐
                         │       INPUT PORT         │
                         │                         │
                         │ WriteTrafficMetric      │
                         │ QueryTrafficMetric       │
                         │ Simulation               │
                         │ Optimization             │
                         │ Alert                    │
                         └───────────┬─────────────┘
                                     │
                                     ▼
                 ┌────────────────────────────────────┐
                 │              CORE                   │
                 │                                    │
                 │       Application Services         │
                 │              │                     │
                 │          Domain Model               │
                 │              │                     │
                 │       Domain Services               │
                 └──────────────┬─────────────────────┘
                                │
                       OUTPUT PORTS
                                │
          ┌─────────────────────┼─────────────────────┐
          │                     │                     │
          ▼                     ▼                     ▼
 TrafficMetricStore      MessagePublisher      SimulationEngine
          │                     │                     │
          ▼                     ▼                     ▼
 ┌────────────────┐     ┌────────────────┐    ┌──────────────┐
 │ TimeSeries DB  │     │ RocketMQ       │    │ SUMO         │
 │                │     │ Spring Events  │    │ CityFlow     │
 │ Influx         │     │ Kafka          │    │ Aimsun       │
 │ Victoria       │     │                │    │ Vissim       │
 │ TDengine       │     │                │    │              │
 └────────────────┘     └────────────────┘    └──────────────┘
```

同时：

```text
                   Composition Root
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
       Inbound        Outbound       External
       Adapters       Adapters       Config
```

这是我建议你们最终固定下来的架构。

---

# 十、模块应该重新定义成什么

我建议最终：

```text
green-traffic
│
├── green-traffic-core
│   ├── domain
│   ├── application
│   └── port
│       ├── input
│       └── output
│
├── green-traffic-adapter-in
│   ├── rest
│   ├── websocket
│   ├── rocketmq
│   ├── spring-event
│   └── simulator-command
│
├── green-traffic-adapter-out
│   ├── influxdb
│   ├── victoria-metrics
│   ├── mysql
│   ├── rocketmq
│   ├── sumo
│   └── ...
│
├── green-traffic-bootstrap
│   ├── application
│   ├── configuration
│   └── composition
│
└── green-traffic-common
    └── （尽量逐步消减）
```

甚至如果项目规模继续扩大，我更建议：

```text
core
adapter-in
adapter-out
bootstrap
```

比现在：

```text
api
infrastructure
simulator
push
common
```

更加符合六边形架构的语义。

---

# 十一、最重要的“环境可替换能力”应该达到什么程度

你提出的要求：

> 不同环境可能使用不同基础设施

我非常赞同，而且应该把它作为架构验收标准，而不是仅仅作为配置项。

最终应该做到：

### 开发环境

```text
REST
 ↓
Core
 ↓
Spring Events
 ↓
InfluxDB
```

### 测试环境

```text
REST
 ↓
Core
 ↓
RocketMQ
 ↓
InfluxDB
```

### VM 环境

```text
REST
 ↓
Core
 ↓
RocketMQ
 ↓
VictoriaMetrics
```

### 未来生产环境

甚至：

```text
REST
 ↓
Core
 ↓
Kafka
 ↓
TDengine
```

然后：

> **Core 完全不修改。**

如果切换：

```text
Influx → Victoria
RocketMQ → Kafka
SUMO → CityFlow
```

需要修改 Core：

```text
❌
```

需要修改 Application Service：

```text
❌
```

需要修改 Domain：

```text
❌
```

最多：

```text
Bootstrap
Configuration
Adapter
```

发生变化。

这才算真正达到了你想要的目标。

---

# 十二、建议建立一个“架构守护测试”

你们已经有：

```text
CoreArchitectureTest
```

这是非常好的基础。

建议继续升级成：

```text
ArchitectureTest
```

至少强制：

### Core 禁止

```text
Spring
Infrastructure
API
RocketMQ
Influx
Victoria
MyBatis
JPA
Redis
```

### Adapter 禁止

```text
Adapter → Adapter
```

比如：

```text
InfluxAdapter → VictoriaAdapter
```

禁止。

### Input Adapter

只能：

```text
Input Adapter
      ↓
Input Port
```

禁止：

```text
Controller → Repository
Controller → Influx
Controller → MyBatis
```

### Output Adapter

只能：

```text
Output Adapter → Output Port
```

实现对应接口。

### Bootstrap

可以：

```text
Core
Adapter
Configuration
```

但 Core 不允许反向依赖 Bootstrap。

---

# 十三、当前问题优先级，我建议这样排

| 优先级    | 问题                                         | 建议                       |
| ------ | ------------------------------------------ | ------------------------ |
| **P0** | ~~MessageSubscriber 放在 Core Output Port~~      | ~~**立即调整**~~                 |
| **P0** | Message Domain/Command/Map 混杂              | **立即统一消息契约**             |
| **P0** | 多套基础设施选择机制                                 | **统一 Adapter Selection** |
| **P0** | Influx/Victoria 业务能力不完全一致                  | **建立 Contract Test**     |
| **P0** | 配置存在数据库密码/Token                            | **立即清理 + 轮换凭据**          |
| **P0** | Prod Messaging 配置自相矛盾                      | **立即修正**                 |
| **P0** | 消息幂等/DLQ实际上只是内存/日志                         | **生产前必须解决**              |
| **P1** | Infrastructure 创建 Core Application Service | 调整 Composition Root      |
| **P1** | SimulationEnginePort 半成品                   | 明确仿真边界                   |
| **P1** | MySQL 没有完整 Port 化                          | 后续补齐                     |
| **P1** | Victoria Adapter 自己管理线程/重试/队列              | 重构                       |
| **P1** | 空 Push 模块                                  | 删除或正式实现                  |
| **P1** | 空 Dashboard Maven 模块                       | 前后端职责分离                  |
| **P1** | Common 有变成垃圾桶风险                            | 控制                       |
| **P2** | ObjectMapper 到处 new                        | 统一                       |
| **P2** | RestTemplate 自建                            | 统一 HTTP Adapter          |
| **P2** | 测试 Controller 混入 API                       | 清理                       |
| **P2** | 重复 import / 注释 / 遗留代码                      | 清理                       |
| **P3** | Checkstyle/Sonar/格式规范                      | 工程化治理                    |

---

# 十四、我建议最近连续处理的 6 个报告

如果这个项目是你接下来要正式推进架构治理，我建议**不要先写《系统架构说明书》**，而是连续产出下面几个报告。

## 报告 1 —— 《GreenTraffic 六边形架构差距分析报告》

**优先级：最高**

内容：

```text
当前架构
       ↓
标准六边形架构
       ↓
差距
       ↓
问题
       ↓
整改方案
```

重点解决：

* Port 方向
* Adapter 分类
* Core 边界
* Bootstrap
* Infrastructure 职责

---

## 报告 2 —— 《GreenTraffic 基础设施可替换性设计报告》

**优先级：最高**

重点研究：

```text
InfluxDB
VictoriaMetrics
MySQL
RocketMQ
Spring Events
SUMO
```

如何变成：

```text
Port
 ↑
Adapter
```

并定义：

```text
环境 → Adapter 组合
```

最终形成：

```text
DEV
TEST
VM
PROD
```

四套架构组合矩阵。

---

## 报告 3 —— 《GreenTraffic 消息架构与可靠性整改报告》

**优先级：最高**

重点解决：

```text
Message
Command
Domain
Map
JSON
```

之间的关系。

同时处理：

```text
幂等
重试
DLQ
消息顺序
消息版本
TraceId
CorrelationId
Schema Version
```

这个报告我建议一定单独做。

---

## 报告 4 —— 《GreenTraffic Core 边界与 Port/Adapter 规范》

**优先级：高**

形成团队以后必须遵守的规则：

```text
什么可以进入 Core
什么不能进入 Core
什么叫 Input Port
什么叫 Output Port
什么叫 Inbound Adapter
什么叫 Outbound Adapter
Bootstrap 做什么
Infrastructure 做什么
```

然后用 ArchUnit 自动检查。

---

## 报告 5 —— 《GreenTraffic 多环境部署与基础设施组合设计》

**优先级：高**

明确：

```text
dev
test
vm
prod
```

到底分别使用：

```text
Storage
Messaging
Cache
Database
Simulator
```

并明确：

```text
配置
Secret
环境变量
K8s ConfigMap
K8s Secret
```

的边界。

---

## 报告 6 —— 《GreenTraffic 架构整改实施计划》

**优先级：高**

最终拆成：

```text
Sprint 1
├── MessageSubscriber
├── Message Contract
├── Secret Cleanup
└── Profile Cleanup

Sprint 2
├── Adapter Refactoring
├── Bootstrap
└── Contract Tests

Sprint 3
├── Simulation
├── MySQL
└── Reliability

Sprint 4
├── Architecture Tests
├── Documentation
└── Production Readiness
```

---

# 十五、如果只允许最近做 3 件事情

那我建议非常明确：

### 第一件：先改消息入口

把：

```text
MessageSubscriber → Core Output Port
```

彻底去掉。

改成：

```text
RocketMQInboundAdapter
          │
          ▼
WriteTrafficMetricUseCase
```

这是**六边形架构最核心的一刀**。

---

### 第二件：统一基础设施选择

把：

```text
@ConditionalOnProperty
+
ObjectProvider
+
Bootstrap fallback
+
各种 profile
```

统一成：

```text
Configuration
      ↓
Adapter Selection
      ↓
Output Port
```

并做：

```text
Influx
Victoria
MySQL
```

的 Contract Test。

---

### 第三件：把安全和配置问题马上处理

尤其：

```text
数据库密码
Influx Token
生产 MQ 地址
```

这些不要等架构整改结束。

**安全问题与架构问题应该并行处理。**

---

# 十六、一个非常重要的判断

你这个项目现在**不是“推倒重来”的状态**。

实际上我认为现在已经有相当不错的六边形基础：

```text
Core
 ├── Domain
 ├── Application
 ├── Input Port
 └── Output Port
```

已经搭起来了。

而且：

```text
CoreArchitectureTest
```

说明你们已经开始主动维护架构边界。

真正的问题是：

> **第一阶段迁移做到了“代码位置调整”，第二阶段需要做“架构语义调整”。**

也就是从：

```text
把 Spring 从 Core 移出去
```

进一步升级到：

```text
真正按照业务方向设计 Port
真正区分 Inbound / Outbound Adapter
真正建立 Composition Root
真正做到基础设施可替换
```

这是完全不同的层次。

---

## 最终目标可以用一句话作为这个项目的架构原则

> **GreenTraffic Core 只表达“城市交通碳排放监测与优化业务”，不表达“这个业务运行在哪个数据库、消息中间件、仿真引擎或 Spring 环境中”。**

做到这一点，你后面无论是：

```text
Influx → VictoriaMetrics
RocketMQ → Kafka
SUMO → CityFlow
MySQL → PostgreSQL
REST → WebSocket
单体 → 微服务
```

都会轻很多。

---

### 关于“当前系统架构文档”

这次我**暂时不建议把当前状态直接整理成正式《当前系统架构文档》**，因为目前的问题已经不是零散的小问题，而是集中在 **Inbound/Outbound Port、消息架构、Composition Root、基础设施选择、可靠性** 这几个核心架构层面。

更合理的顺序是：

**① 六边形差距报告 → ② 消息/基础设施整改 → ③ 架构守护规则 → ④ 目标架构 → ⑤ 正式《GreenTraffic 系统架构设计文档》**

否则现在写出来的“当前架构文档”很容易在整改后一个月就过期。

如果按照架构治理的实际工作量判断，**目前最值得立即动手的不是重构全部代码，而是先做 P0 的 7 项；其中 MessageSubscriber、消息契约、基础设施选择机制这三个应该作为第一批重构的主线。**
